# URL base da API do backend do Fees Monitor.
# Deixe vazio (ou apague o .env) para usar o backend simulado de src/lib/mockBackend.ts,
# que gera dado de gas com a mesma forma do real e não precisa de Docker.
VITE_API_URL=http://localhost:3000
